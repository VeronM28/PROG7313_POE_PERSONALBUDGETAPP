<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fragmentContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".LaunchPageActivity">

    <!-- Launch Screen Content (initially visible) -->
    <LinearLayout
        android:id="@+id/launchContent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        android:background="@android:color/white">

        <!-- Logo -->
        <ImageView
            android:id="@+id/logo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="50dp"
            android:layout_marginBottom="30dp"
            android:src="@drawable/mzansi_money"
            android:contentDescription="Mzansi Money Logo"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter" />

        <!-- Login Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_launch_login"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="20dp"
            android:background="@drawable/gradient_background"
            android:backgroundTint="@null"
            android:text="Login"
            android:textColor="@android:color/black"
            android:visibility="visible"
            app:backgroundTint="@null"
            app:cornerRadius="50dp" />

        <!-- Register Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_launch_reg"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="20dp"
            android:background="@drawable/gradient_background"
            android:backgroundTint="@null"
            android:text="Register"
            android:textColor="@android:color/black"
            android:visibility="visible"
            app:backgroundTint="@null"
            app:cornerRadius="50dp" />

        <!-- Optional Help Text -->
        <TextView
            android:id="@+id/txtViewHelp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Need Help?"
            android:textColor="#757575"
            android:layout_gravity="center"
            android:layout_marginTop="10dp" />
    </LinearLayout>

</FrameLayout>





